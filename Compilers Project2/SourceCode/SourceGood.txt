program example2(input, output);
var temp: real;
var hold: integer;
procedure lcd(b: integer);
	procedure gcd(a: real);
	begin
	end;
begin
	if b = 0 then lcd := temp
	call write(x, y)
	call read(x, y);
end.